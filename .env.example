# Copy this file to ".env" and fill it with real values.
# Do not commit real secrets. Keep this file aligned with "src/env.js" and README.md.

# Required to run
DATABASE_URL="file:./db.sqlite"
# Site URL: single public base for the app (Mercado Pago back_urls, links, NextAuth fallback).
# Local dev: http://localhost:3000
# Local dev + tunnel (ngrok, etc.): set both URL and WEBHOOK_URL to the same https://... tunnel origin so MP redirects and webhooks hit your machine.
# Vercel/production: production site origin only (see README).
URL="http://localhost:3000"
MERCADOPAGO_TOKEN=""
# Webhook URL: public base Mercado Pago calls (path /api/webhook is appended in code). Must be reachable from the internet when testing real webhooks.
WEBHOOK_URL="http://localhost:3000"
CRON_SECRET=""

# Required in production
# Generate NEXTAUTH_SECRET with: openssl rand -base64 32
NEXTAUTH_SECRET=""
# Hash format: scrypt:<salt>:<derived-key-hex>
# Generate with: pnpm admin:hash -- "your-password"
ADMIN_PASSWORD_HASH=""

# Optional internal access
EMPLOYEE_PASSWORD_HASH=""

# Payments and webhooks
WEBHOOK_SECRET=""

# Public prices and behavior
NEXT_PUBLIC_MAX_INTENDED_DAYS="30"
NEXT_PUBLIC_VOUCHER_PRICE="50"
NEXT_PUBLIC_VOUCHER_POOL_PRICE="70"
NEXT_PUBLIC_POOL_VOUCHER_PRICE="70"
NEXT_PUBLIC_ALERT_MESSAGE=""
NEXT_PUBLIC_DATA_SAVER="false"
NEXT_PUBLIC_ENABLE_ANALYTICS="false"

# Optional marketing and notifications
FACEBOOK_PIXEL_ID=""
FACEBOOK_ACCESS_TOKEN=""
GOOGLE_ANALYTICS_MEASUREMENT_ID=""
GOOGLE_ANALYTICS_API_SECRET=""
NEXT_PUBLIC_FACEBOOK_PIXEL_ID=""
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
